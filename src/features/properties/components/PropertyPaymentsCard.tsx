import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export const PropertyPaymentsCard = () => {
  return (
    <Card className="w-full">
      <CardHeader>
        <CardTitle>Historial de Pagos Mensuales</CardTitle>
      </CardHeader>
      <CardContent>
        <p>card4</p>
      </CardContent>
    </Card>
  );
};
