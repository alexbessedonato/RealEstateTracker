import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export const PropertyTenantsCard = () => {
  return (
    <Card className="w-full">
      <CardHeader>
        <CardTitle>Inquilinos</CardTitle>
      </CardHeader>
      <CardContent>
        <p>card3</p>
      </CardContent>
    </Card>
  );
};
