import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export const PropertyManagersCard = () => {
  return (
    <Card className="w-full">
      <CardHeader>
        <CardTitle>Managers</CardTitle>
      </CardHeader>
      <CardContent>
        <p>card2</p>
      </CardContent>
    </Card>
  );
};
